# Problem 1: Count Operations

for i in range(n):        # runs n times (outer loop)
    for j in range(n):    # runs n times for each i (inner loop)
        print(i, j)       # runs n * n times = n^2

# Total operations = n * n = n^2
# Time Complexity = O(n^2)

#outer = n
#inner = n
#total = n * n = n^2
#→ O(n^2)
